[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests
# app/engine.py가 `from . import behaviors, db`를 쓰므로 테스트는 `from app import engine`로
# 패키지 임포트해야 한다 → 프로젝트 루트가 sys.path에 있어야 함.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
